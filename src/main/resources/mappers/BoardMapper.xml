<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
    PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <mapper namespace="kr.ac.smu.cs.comnet.mapper.BoardMapper">
    	<select id="selectList" resultType="BoardVO">
    		SELECT @rownum:=@rownum+1 as rowNum, Board.* FROM Board,
    		(SELECT @rownum:=0)r;
    	</select>
		<insert id="register">
			INSERT INTO Board(title, content, deadline, partner_limit, contact, reg_date, uid)
			VALUES(#{title}, #{content}, #{deadline}, #{partner_limit}, #{contact}, #{reg_date}, #{uid})
		</insert>
		<select id="select" resultType="int">
			SELECT bid FROM Board where reg_date=#{reg_date}
		</select>
		<select id="selectSuitableList" resultType="BoardVO">
    		SELECT DISTINCT Board.* FROM Board, conn_bf, conn_bl
    		<choose>
    			<when test="languageList.size()>0 and fieldList.size()>0">
	    			WHERE (conn_bf.bid=Board.bid AND conn_bf.fid IN 
					<foreach item="fid" collection="fieldList" open="(" close=")" separator=",">
						#{fid}
					</foreach>
						   )
					OR (conn_bl.bid=Board.bid AND conn_bl.lid IN
					<foreach item="lid" collection="languageList" open="(" close=")" separator=",">
						#{lid}
					</foreach>
						  )
    			</when>
    			<when test="fieldList.size()>0">
	    			WHERE (conn_bf.bid=Board.bid AND conn_bf.fid IN 
					<foreach item="fid" collection="fieldList" open="(" close=")" separator=",">
						#{fid}
					</foreach>
						   )
    			</when>
    			<when test="languageList.size()>0">
	    			WHERE (conn_bl.bid=Board.bid AND conn_bl.lid IN
					<foreach item="lid" collection="languageList" open="(" close=")" separator=",">
						#{lid}
					</foreach>
						  )
    			</when>
    		</choose>
			GROUP BY Board.bid
    	</select>
    </mapper>